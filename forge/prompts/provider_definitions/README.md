# Provider Prompt Definitions

This is the canonical location for versioned Provider Prompt Definitions.
Each definition identifies the provider-specific rendering contract used to
derive a Runtime Prompt and contributes stable provenance. Increment 1.9
models definition identity and version only; it does not add a provider,
template, or runtime invocation.
