# Provider Metadata

This is the canonical documentation location for immutable provider metadata.
Each registered provider declares its identity, version, type, capabilities,
reasoning modes, qualification state, and status through `AIProviderMetadata`.
No concrete provider belongs here until a separately authorized increment.
