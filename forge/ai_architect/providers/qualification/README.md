# Provider Qualification

This is the canonical documentation location for repository-owned provider
qualification. `ProviderQualification` records the provider identity, version,
state, and repository reference. Qualification never grants execution
authority; only active `QUALIFIED` declarations are eligible for selection.
